import React from 'react';

interface Props {
    className?: string;
}

export const MiddleStatistics: React.FC<Props> = ({ className }) => {
    return <div className={className}>MiddleStatistics</div>;
};
